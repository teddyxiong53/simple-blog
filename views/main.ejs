<% if (mainType === 'posts') { %>
<%- include('posts.ejs'); %>
<% } else if (mainType === 'signup') { %>
    signup
<% } else if (mainType === 'signin') { %>
    signin
<% } else  { %>
    页面不存在
<% } %>